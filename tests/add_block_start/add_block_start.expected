# >>> Inserted at top
import logging
logging.basicConfig(level=logging.INFO)

# hello.py


def greet(name):
    print(f"Hello, {name}!")


class Greeter:
    def __init__(self, name):
        print(f'Hello {name}')
